package test:sigwrite-f32@0.1.0;

interface app-component {
  resource app {
    constructor();
    mount: func(root: u32);
    unmount: func();
    get-value: func() -> option<f32>;
    set-value: func(value: option<f32>);
  }
}

world sigwrite-f32 {
  import yel:ui/dom@0.1.0;
  export app-component;
  export yel:ui/dispatch@0.1.0;
}
