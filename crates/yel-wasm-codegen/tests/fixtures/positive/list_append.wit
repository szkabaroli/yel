package yel:listapp@0.1.0;

interface app-component {
  resource app {
    constructor();
    mount: func(root: u32);
    unmount: func();
    get-items: func() -> list<s32>;
    set-items: func(value: list<s32>);
    get-next: func() -> s32;
    set-next: func(value: s32);
  }
}

world listapp {
  import yel:ui/dom@0.1.0;
  export app-component;
  export yel:ui/dispatch@0.1.0;
}
