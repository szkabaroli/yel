package yel:listoptlist@0.1.0;

interface app-component {
  resource app {
    constructor();
    mount: func(root: u32);
    unmount: func();
    get-items: func() -> list<option<list<s32>>>;
    set-items: func(value: list<option<list<s32>>>);
  }
}

world listoptlist {
  import yel:ui/dom@0.1.0;
  export app-component;
  export yel:ui/dispatch@0.1.0;
}
